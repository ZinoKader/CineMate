package main.controllers.contract;

import javafx.scene.web.WebView;

/**
 * Contract for view-controllers that need to display a trailer
 */
public interface TrailerView {

    WebView getTrailerView();

}
