package main.config;

/**
 * Marker interface for more generalized "property" key type.
 * Currently we only have a settings key type but in the future, when more key-value pairs are added to the properties
 * this will be needed
 */
public interface PropertyKey {
}
